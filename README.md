#Starting steps

```
make empty folder
git init
npm init -y
npm install -s express
touch .gitignore    --OR--   echo "node_modules" > .gitignore
touch app.js
change main.js in package.json to app.js
node app.js

```